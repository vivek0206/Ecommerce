<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ecommerce.ecommerce">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SubCategoryList"></activity>
        <activity android:name=".activity.ProductDetailActivity"></activity>
        <activity android:name=".activity.tempActivity" />
        <activity android:name=".activity.DetailCategoryList" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.SignUp" />
        <activity android:name=".activity.verifyPhone" />
        <activity android:name=".activity.DeliveryAddress" />
        <activity android:name=".activity.PersonalInfo" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>